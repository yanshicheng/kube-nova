// Code generated by goctl. DO NOT EDIT.
// versions:
//  goctl version: 1.7.3

package model

import (
	"context"
	"database/sql"
	"fmt"
	"strings"
	"time"

	"github.com/zeromicro/go-zero/core/stores/builder"
	"github.com/zeromicro/go-zero/core/stores/cache"
	"github.com/zeromicro/go-zero/core/stores/sqlc"
	"github.com/zeromicro/go-zero/core/stores/sqlx"
	"github.com/zeromicro/go-zero/core/stringx"
)

var (
	alertNotificationsFieldNames          = builder.RawFieldNames(&AlertNotifications{})
	alertNotificationsRows                = strings.Join(alertNotificationsFieldNames, ",")
	alertNotificationsRowsExpectAutoSet   = strings.Join(stringx.Remove(alertNotificationsFieldNames, "`id`", "`create_at`", "`create_time`", "`created_at`", "`update_at`", "`update_time`", "`updated_at`"), ",")
	alertNotificationsRowsWithPlaceHolder = strings.Join(stringx.Remove(alertNotificationsFieldNames, "`id`", "`create_at`", "`create_time`", "`created_at`", "`update_at`", "`update_time`", "`updated_at`"), "=?,") + "=?"

	cacheIkubeopsAlertNotificationsIdPrefix   = "cache:ikubeops:alertNotifications:id:"
	cacheIkubeopsAlertNotificationsUuidPrefix = "cache:ikubeops:alertNotifications:uuid:"
)

type (
	alertNotificationsModel interface {
		Insert(ctx context.Context, data *AlertNotifications) (sql.Result, error)

		FindOne(ctx context.Context, id uint64) (*AlertNotifications, error)
		Search(ctx context.Context, orderStr string, isAsc bool, page, pageSize uint64, queryStr string, args ...any) ([]*AlertNotifications, uint64, error)
		SearchNoPage(ctx context.Context, orderStr string, isAsc bool, queryStr string, args ...any) ([]*AlertNotifications, error)
		FindOneByUuid(ctx context.Context, uuid string) (*AlertNotifications, error)
		Update(ctx context.Context, data *AlertNotifications) error
		Delete(ctx context.Context, id uint64) error
		DeleteSoft(ctx context.Context, id uint64) error
		TransCtx(ctx context.Context, fn func(context.Context, sqlx.Session) error) error
		TransOnSql(ctx context.Context, session sqlx.Session, id uint64, sqlStr string, args ...any) (sql.Result, error)
		ExecSql(ctx context.Context, id uint64, sqlStr string, args ...any) (sql.Result, error)
	}

	defaultAlertNotificationsModel struct {
		sqlc.CachedConn
		table string
	}

	AlertNotifications struct {
		Id          uint64    `db:"id"`           // 主键ID
		Uuid        string    `db:"uuid"`         // 通知记录UUID
		InstanceId  uint64    `db:"instance_id"`  // 告警实例ID
		GroupId     uint64    `db:"group_id"`     // 告警组ID
		Severity    string    `db:"severity"`     // 告警级别
		ChannelId   uint64    `db:"channel_id"`   // 告警渠道ID
		ChannelType string    `db:"channel_type"` // 渠道类型
		SendFormat  string    `db:"send_format"`  // 发送格式(markdown, html, text)
		Recipients  string    `db:"recipients"`   // 接收人列表(JSON格式)
		Subject     string    `db:"subject"`      // 通知主题
		Content     string    `db:"content"`      // 发送内容
		Status      string    `db:"status"`       // 发送状态(pending, sending, success, failed, cancelled)
		ErrorMsg    string    `db:"error_msg"`    // 错误信息
		SentAt      time.Time `db:"sent_at"`      // 发送时间
		Response    string    `db:"response"`     // 渠道响应信息
		CostMs      uint64    `db:"cost_ms"`      // 发送耗时(毫秒)
		CreatedBy   string    `db:"created_by"`   // 创建人
		UpdatedBy   string    `db:"updated_by"`   // 更新人
		CreatedAt   time.Time `db:"created_at"`   // 创建时间
		UpdatedAt   time.Time `db:"updated_at"`   // 更新时间
		IsDeleted   int64     `db:"is_deleted"`   // 是否删除
	}
)

func newAlertNotificationsModel(conn sqlx.SqlConn, c cache.CacheConf, opts ...cache.Option) *defaultAlertNotificationsModel {
	return &defaultAlertNotificationsModel{
		CachedConn: sqlc.NewConn(conn, c, opts...),
		table:      "`alert_notifications`",
	}
}

func (m *defaultAlertNotificationsModel) Delete(ctx context.Context, id uint64) error {
	data, err := m.FindOne(ctx, id)
	if err != nil {
		return err
	}

	ikubeopsAlertNotificationsIdKey := fmt.Sprintf("%s%v", cacheIkubeopsAlertNotificationsIdPrefix, id)
	ikubeopsAlertNotificationsUuidKey := fmt.Sprintf("%s%v", cacheIkubeopsAlertNotificationsUuidPrefix, data.Uuid)
	_, err = m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (result sql.Result, err error) {
		query := fmt.Sprintf("delete from %s where `id` = ?", m.table)
		return conn.ExecCtx(ctx, query, id)
	}, ikubeopsAlertNotificationsIdKey, ikubeopsAlertNotificationsUuidKey)
	return err
}

func (m *defaultAlertNotificationsModel) DeleteSoft(ctx context.Context, id uint64) error {
	data, err := m.FindOne(ctx, id)
	if err != nil {
		return err
	}
	// 如果记录已软删除，无需再次删除
	if data.IsDeleted == 1 {
		return nil
	}
	ikubeopsAlertNotificationsIdKey := fmt.Sprintf("%s%v", cacheIkubeopsAlertNotificationsIdPrefix, id)
	ikubeopsAlertNotificationsUuidKey := fmt.Sprintf("%s%v", cacheIkubeopsAlertNotificationsUuidPrefix, data.Uuid)
	_, err = m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (result sql.Result, err error) {
		query := fmt.Sprintf("update %s set `is_deleted` = 1 where `id` = ?", m.table)
		return conn.ExecCtx(ctx, query, id)
	}, ikubeopsAlertNotificationsIdKey, ikubeopsAlertNotificationsUuidKey)
	return err
}

func (m *defaultAlertNotificationsModel) TransCtx(ctx context.Context, fn func(context.Context, sqlx.Session) error) error {
	return m.TransactCtx(ctx, func(ctx context.Context, session sqlx.Session) error {
		return fn(ctx, session)
	})
}

func (m *defaultAlertNotificationsModel) TransOnSql(ctx context.Context, session sqlx.Session, id uint64, sqlStr string, args ...any) (sql.Result, error) {
	query := strings.ReplaceAll(sqlStr, "{table}", m.table)
	// 如果 id != 0 并且启用了缓存逻辑
	if !isZeroValue(id) {
		// 查询数据（如果需要，确保数据存在）
		data, err := m.FindOne(ctx, id)
		if err != nil {
			return nil, err
		}

		// 缓存相关处理

		ikubeopsAlertNotificationsIdKey := fmt.Sprintf("%s%v", cacheIkubeopsAlertNotificationsIdPrefix, id)
		ikubeopsAlertNotificationsUuidKey := fmt.Sprintf("%s%v", cacheIkubeopsAlertNotificationsUuidPrefix, data.Uuid) // 处理缓存逻辑，例如删除或更新缓存
		// 执行带缓存处理的 SQL 操作
		return m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (sql.Result, error) {
			return session.ExecCtx(ctx, query, args...)
		}, ikubeopsAlertNotificationsIdKey, ikubeopsAlertNotificationsUuidKey) // 传递缓存相关的键值

	}

	// 如果 id == 0 或不需要缓存，直接执行 SQL
	return m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (sql.Result, error) {
		return session.ExecCtx(ctx, query, args...)
	})
}

func (m *defaultAlertNotificationsModel) ExecSql(ctx context.Context, id uint64, sqlStr string, args ...any) (sql.Result, error) {
	// 如果 id != 0 并且启用了缓存逻辑
	query := strings.ReplaceAll(sqlStr, "{table}", m.table)
	if !isZeroValue(id) {
		// 缓存相关处理

		// 查询数据（如果需要，确保数据存在）
		data, err := m.FindOne(ctx, id)
		if err != nil {
			return nil, err
		}

		ikubeopsAlertNotificationsIdKey := fmt.Sprintf("%s%v", cacheIkubeopsAlertNotificationsIdPrefix, id)
		ikubeopsAlertNotificationsUuidKey := fmt.Sprintf("%s%v", cacheIkubeopsAlertNotificationsUuidPrefix, data.Uuid) // 处理缓存逻辑，例如删除或更新缓存
		// 执行带缓存处理的 SQL 操作
		return m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (sql.Result, error) {
			return conn.ExecCtx(ctx, query, args...)
		}, ikubeopsAlertNotificationsIdKey, ikubeopsAlertNotificationsUuidKey) // 传递缓存相关的键值

	}

	// 如果 id == 0 或不需要缓存，直接执行 SQL
	return m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (sql.Result, error) {
		return conn.ExecCtx(ctx, query, args...)
	})
}
func (m *defaultAlertNotificationsModel) FindOne(ctx context.Context, id uint64) (*AlertNotifications, error) {
	ikubeopsAlertNotificationsIdKey := fmt.Sprintf("%s%v", cacheIkubeopsAlertNotificationsIdPrefix, id)
	var resp AlertNotifications
	err := m.QueryRowCtx(ctx, &resp, ikubeopsAlertNotificationsIdKey, func(ctx context.Context, conn sqlx.SqlConn, v any) error {
		query := fmt.Sprintf("select %s from %s where `id` = ? AND `is_deleted` = 0 limit 1", alertNotificationsRows, m.table)
		return conn.QueryRowCtx(ctx, v, query, id)
	})
	switch err {
	case nil:
		return &resp, nil
	case sqlc.ErrNotFound:
		return nil, ErrNotFound
	default:
		return nil, err
	}
}

func (m *defaultAlertNotificationsModel) Search(ctx context.Context, orderStr string, isAsc bool, page, pageSize uint64, queryStr string, args ...any) ([]*AlertNotifications, uint64, error) {
	// 确保分页参数有效
	if page < 1 {
		page = 1
	}
	if pageSize < 1 {
		pageSize = 20
	}

	// 构造查询条件
	// 添加 `is_deleted` = 0 条件，保证只查询未软删除数据
	// 初始化 WHERE 子句
	where := "WHERE `is_deleted` = 0"
	if queryStr != "" {
		where = fmt.Sprintf("WHERE %s AND `is_deleted` = 0", queryStr)
	}

	// 根据 isAsc 参数确定排序方式
	sortDirection := "ASC"
	if !isAsc {
		sortDirection = "DESC"
	}

	// 如果用户未指定排序字段，则默认使用 id
	if orderStr == "" {
		orderStr = fmt.Sprintf("ORDER BY id %s", sortDirection)
	} else {
		orderStr = strings.TrimSpace(orderStr)
		if !strings.HasPrefix(strings.ToUpper(orderStr), "ORDER BY") {
			orderStr = "ORDER BY " + orderStr
		}
		orderStr = fmt.Sprintf("%s %s", orderStr, sortDirection)
	}

	countQuery := fmt.Sprintf("SELECT COUNT(1) FROM %s %s", m.table, where)

	var total uint64
	var resp []*AlertNotifications
	err := m.QueryRowNoCacheCtx(ctx, &total, countQuery, args...)
	if err != nil {
		return nil, 0, err
	}
	if total == 0 {
		// 无匹配记录
		return resp, 0, ErrNotFound
	}
	offset := (page - 1) * pageSize
	dataQuery := fmt.Sprintf("SELECT %s FROM %s %s %s LIMIT %d,%d", alertNotificationsRows, m.table, where, orderStr, offset, pageSize)

	err = m.QueryRowsNoCacheCtx(ctx, &resp, dataQuery, args...)
	if err != nil {
		return nil, 0, err
	}

	return resp, total, nil
}

func (m *defaultAlertNotificationsModel) SearchNoPage(ctx context.Context, orderStr string, isAsc bool, queryStr string, args ...any) ([]*AlertNotifications, error) {
	// 处理 nil 参数
	if args == nil {
		args = []any{}
	}
	// 初始化 WHERE 子句
	where := "WHERE `is_deleted` = 0"
	// 处理查询条件和参数
	var finalArgs []any
	if queryStr != "" {
		where = fmt.Sprintf("WHERE %s AND `is_deleted` = 0", queryStr)
		// 只有当有查询条件时才使用参数
		if len(args) > 0 {
			finalArgs = args
		}
	}

	// 根据 isAsc 参数确定排序方式
	sortDirection := "ASC"
	if !isAsc {
		sortDirection = "DESC"
	}
	// 如果用户未指定排序字段，则默认使用 id
	if orderStr == "" {
		orderStr = fmt.Sprintf("ORDER BY id %s", sortDirection)
	} else {
		orderStr = strings.TrimSpace(orderStr)
		if !strings.HasPrefix(strings.ToUpper(orderStr), "ORDER BY") {
			orderStr = "ORDER BY " + orderStr
		}
		orderStr = fmt.Sprintf("%s %s", orderStr, sortDirection)
	}
	dataQuery := fmt.Sprintf("SELECT %s FROM %s %s %s", alertNotificationsRows, m.table, where, orderStr)
	var resp []*AlertNotifications
	// 根据是否有参数来调用
	var err error
	if len(finalArgs) > 0 {
		err = m.QueryRowsNoCacheCtx(ctx, &resp, dataQuery, finalArgs...)
	} else {
		err = m.QueryRowsNoCacheCtx(ctx, &resp, dataQuery)
	}
	switch err {
	case nil:
		return resp, nil
	case sqlx.ErrNotFound:
		return nil, ErrNotFound
	default:
		return nil, err
	}
}
func (m *defaultAlertNotificationsModel) FindOneByUuid(ctx context.Context, uuid string) (*AlertNotifications, error) {
	ikubeopsAlertNotificationsUuidKey := fmt.Sprintf("%s%v", cacheIkubeopsAlertNotificationsUuidPrefix, uuid)
	var resp AlertNotifications
	err := m.QueryRowIndexCtx(ctx, &resp, ikubeopsAlertNotificationsUuidKey, m.formatPrimary, func(ctx context.Context, conn sqlx.SqlConn, v any) (i any, e error) {
		query := fmt.Sprintf("select %s from %s where `uuid` = ? AND `is_deleted` = 0  limit 1", alertNotificationsRows, m.table)
		if err := conn.QueryRowCtx(ctx, &resp, query, uuid); err != nil {
			return nil, err
		}
		return resp.Id, nil
	}, m.queryPrimary)
	switch err {
	case nil:
		return &resp, nil
	case sqlc.ErrNotFound:
		return nil, ErrNotFound
	default:
		return nil, err
	}
}

func (m *defaultAlertNotificationsModel) Insert(ctx context.Context, data *AlertNotifications) (sql.Result, error) {
	ikubeopsAlertNotificationsIdKey := fmt.Sprintf("%s%v", cacheIkubeopsAlertNotificationsIdPrefix, data.Id)
	ikubeopsAlertNotificationsUuidKey := fmt.Sprintf("%s%v", cacheIkubeopsAlertNotificationsUuidPrefix, data.Uuid)
	ret, err := m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (result sql.Result, err error) {
		query := fmt.Sprintf("insert into %s (%s) values (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)", m.table, alertNotificationsRowsExpectAutoSet)
		return conn.ExecCtx(ctx, query, data.Uuid, data.InstanceId, data.GroupId, data.Severity, data.ChannelId, data.ChannelType, data.SendFormat, data.Recipients, data.Subject, data.Content, data.Status, data.ErrorMsg, data.SentAt, data.Response, data.CostMs, data.CreatedBy, data.UpdatedBy, data.IsDeleted)
	}, ikubeopsAlertNotificationsIdKey, ikubeopsAlertNotificationsUuidKey)
	return ret, err
}

func (m *defaultAlertNotificationsModel) Update(ctx context.Context, newData *AlertNotifications) error {
	data, err := m.FindOne(ctx, newData.Id)
	if err != nil {
		return err
	}

	ikubeopsAlertNotificationsIdKey := fmt.Sprintf("%s%v", cacheIkubeopsAlertNotificationsIdPrefix, data.Id)
	ikubeopsAlertNotificationsUuidKey := fmt.Sprintf("%s%v", cacheIkubeopsAlertNotificationsUuidPrefix, data.Uuid)
	_, err = m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (result sql.Result, err error) {
		query := fmt.Sprintf("update %s set %s where `id` = ?", m.table, alertNotificationsRowsWithPlaceHolder)
		return conn.ExecCtx(ctx, query, newData.Uuid, newData.InstanceId, newData.GroupId, newData.Severity, newData.ChannelId, newData.ChannelType, newData.SendFormat, newData.Recipients, newData.Subject, newData.Content, newData.Status, newData.ErrorMsg, newData.SentAt, newData.Response, newData.CostMs, newData.CreatedBy, newData.UpdatedBy, newData.IsDeleted, newData.Id)
	}, ikubeopsAlertNotificationsIdKey, ikubeopsAlertNotificationsUuidKey)
	return err
}

func (m *defaultAlertNotificationsModel) formatPrimary(primary any) string {
	return fmt.Sprintf("%s%v", cacheIkubeopsAlertNotificationsIdPrefix, primary)
}

func (m *defaultAlertNotificationsModel) queryPrimary(ctx context.Context, conn sqlx.SqlConn, v, primary any) error {
	query := fmt.Sprintf("select %s from %s where `id` = ? AND `is_deleted` = 0 limit 1", alertNotificationsRows, m.table)
	return conn.QueryRowCtx(ctx, v, query, primary)
}

func (m *defaultAlertNotificationsModel) tableName() string {
	return m.table
}
