// Code generated by goctl. DO NOT EDIT.
// versions:
//  goctl version: 1.9.2

package model

import (
	"context"
	"database/sql"
	"fmt"
	"strings"
	"time"

	"github.com/zeromicro/go-zero/core/stores/builder"
	"github.com/zeromicro/go-zero/core/stores/cache"
	"github.com/zeromicro/go-zero/core/stores/sqlc"
	"github.com/zeromicro/go-zero/core/stores/sqlx"
	"github.com/zeromicro/go-zero/core/stringx"
)

var (
	sysUserPlatformFieldNames          = builder.RawFieldNames(&SysUserPlatform{})
	sysUserPlatformRows                = strings.Join(sysUserPlatformFieldNames, ",")
	sysUserPlatformRowsExpectAutoSet   = strings.Join(stringx.Remove(sysUserPlatformFieldNames, "`id`", "`create_at`", "`create_time`", "`created_at`", "`update_at`", "`update_time`", "`updated_at`"), ",")
	sysUserPlatformRowsWithPlaceHolder = strings.Join(stringx.Remove(sysUserPlatformFieldNames, "`id`", "`create_at`", "`create_time`", "`created_at`", "`update_at`", "`update_time`", "`updated_at`"), "=?,") + "=?"

	cacheKubeNovaSysUserPlatformIdPrefix               = "cache:kubeNova:sysUserPlatform:id:"
	cacheKubeNovaSysUserPlatformUserIdPlatformIdPrefix = "cache:kubeNova:sysUserPlatform:userId:platformId:"
)

type (
	sysUserPlatformModel interface {
		Insert(ctx context.Context, data *SysUserPlatform) (sql.Result, error)
		FindOne(ctx context.Context, id uint64) (*SysUserPlatform, error)
		Search(ctx context.Context, orderStr string, isAsc bool, page, pageSize uint64, queryStr string, args ...any) ([]*SysUserPlatform, uint64, error)
		SearchNoPage(ctx context.Context, orderStr string, isAsc bool, queryStr string, args ...any) ([]*SysUserPlatform, error)
		FindOneByUserIdPlatformId(ctx context.Context, userId uint64, platformId uint64) (*SysUserPlatform, error)
		Update(ctx context.Context, data *SysUserPlatform) error
		Delete(ctx context.Context, id uint64) error
		DeleteSoft(ctx context.Context, id uint64) error
		TransCtx(ctx context.Context, fn func(context.Context, sqlx.Session) error) error
		TransOnSql(ctx context.Context, session sqlx.Session, id uint64, sqlStr string, args ...any) (sql.Result, error)
		ExecSql(ctx context.Context, id uint64, sqlStr string, args ...any) (sql.Result, error)
	}

	defaultSysUserPlatformModel struct {
		sqlc.CachedConn
		table string
	}

	SysUserPlatform struct {
		Id         uint64         `db:"id"`          // 自增主键
		UserId     uint64         `db:"user_id"`     // 用户ID（关联sys_user表）
		PlatformId uint64         `db:"platform_id"` // 平台ID（关联sys_platform表）
		IsEnable   int64          `db:"is_enable"`   // 是否启用（0:禁用 1:启用）
		Status     int64          `db:"status"`      // 状态（0:停用 1:启用）
		IsDeleted  int64          `db:"is_deleted"`  // 是否删除（0:正常 1:已删除）
		CreateTime time.Time      `db:"create_time"` // 创建时间
		UpdateTime time.Time      `db:"update_time"` // 更新时间
		CreateBy   sql.NullString `db:"create_by"`   // 创建人
		UpdateBy   sql.NullString `db:"update_by"`   // 更新人
	}
)

func newSysUserPlatformModel(conn sqlx.SqlConn, c cache.CacheConf, opts ...cache.Option) *defaultSysUserPlatformModel {
	return &defaultSysUserPlatformModel{
		CachedConn: sqlc.NewConn(conn, c, opts...),
		table:      "`sys_user_platform`",
	}
}

func (m *defaultSysUserPlatformModel) Delete(ctx context.Context, id uint64) error {
	data, err := m.FindOne(ctx, id)
	if err != nil {
		return err
	}

	kubeNovaSysUserPlatformIdKey := fmt.Sprintf("%s%v", cacheKubeNovaSysUserPlatformIdPrefix, id)
	kubeNovaSysUserPlatformUserIdPlatformIdKey := fmt.Sprintf("%s%v:%v", cacheKubeNovaSysUserPlatformUserIdPlatformIdPrefix, data.UserId, data.PlatformId)
	_, err = m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (result sql.Result, err error) {
		query := fmt.Sprintf("delete from %s where `id` = ?", m.table)
		return conn.ExecCtx(ctx, query, id)
	}, kubeNovaSysUserPlatformIdKey, kubeNovaSysUserPlatformUserIdPlatformIdKey)
	return err
}

func (m *defaultSysUserPlatformModel) DeleteSoft(ctx context.Context, id uint64) error {
	data, err := m.FindOne(ctx, id)
	if err != nil {
		return err
	}
	// 如果记录已软删除，无需再次删除
	if data.IsDeleted == 1 {
		return nil
	}
	kubeNovaSysUserPlatformIdKey := fmt.Sprintf("%s%v", cacheKubeNovaSysUserPlatformIdPrefix, id)
	kubeNovaSysUserPlatformUserIdPlatformIdKey := fmt.Sprintf("%s%v:%v", cacheKubeNovaSysUserPlatformUserIdPlatformIdPrefix, data.UserId, data.PlatformId)
	_, err = m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (result sql.Result, err error) {
		query := fmt.Sprintf("update %s set `is_deleted` = 1 where `id` = ?", m.table)
		return conn.ExecCtx(ctx, query, id)
	}, kubeNovaSysUserPlatformIdKey, kubeNovaSysUserPlatformUserIdPlatformIdKey)
	return err
}

func (m *defaultSysUserPlatformModel) TransCtx(ctx context.Context, fn func(context.Context, sqlx.Session) error) error {
	return m.TransactCtx(ctx, func(ctx context.Context, session sqlx.Session) error {
		return fn(ctx, session)
	})
}

func (m *defaultSysUserPlatformModel) TransOnSql(ctx context.Context, session sqlx.Session, id uint64, sqlStr string, args ...any) (sql.Result, error) {
	// 校验 SQL 字符串是否包含表名占位符
	// 如果没有 {table} 占位符，直接使用原始 SQL（兼容直接写表名的情况）
	var query string
	if strings.Contains(sqlStr, "{table}") {
		query = strings.ReplaceAll(sqlStr, "{table}", m.table)
	} else {
		query = sqlStr
	}

	// 如果 id 不为零值并且需要处理缓存逻辑
	if !isZeroValue(id) {

		// 查询数据以获取缓存键（如果需要）
		data, err := m.FindOne(ctx, id)
		if err != nil {
			return nil, err
		}

		// 处理缓存逻辑
		kubeNovaSysUserPlatformIdKey := fmt.Sprintf("%s%v", cacheKubeNovaSysUserPlatformIdPrefix, id)
		kubeNovaSysUserPlatformUserIdPlatformIdKey := fmt.Sprintf("%s%v:%v", cacheKubeNovaSysUserPlatformUserIdPlatformIdPrefix, data.UserId, data.PlatformId)
		// 执行带缓存处理的 SQL 操作
		return m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (sql.Result, error) {
			return session.ExecCtx(ctx, query, args...)
		}, kubeNovaSysUserPlatformIdKey, kubeNovaSysUserPlatformUserIdPlatformIdKey)

	}

	// 如果 id 为零值，直接通过 session 执行 SQL（无需处理缓存）
	return session.ExecCtx(ctx, query, args...)
}

func (m *defaultSysUserPlatformModel) ExecSql(ctx context.Context, id uint64, sqlStr string, args ...any) (sql.Result, error) {
	// 校验 SQL 字符串是否包含表名占位符
	// 如果没有 {table} 占位符，直接使用原始 SQL（兼容直接写表名的情况）
	var query string
	if strings.Contains(sqlStr, "{table}") {
		query = strings.ReplaceAll(sqlStr, "{table}", m.table)
	} else {
		query = sqlStr
	}

	// 如果 id 不为零值并且需要处理缓存逻辑
	if !isZeroValue(id) {

		// 查询数据以获取缓存键（如果需要）
		data, err := m.FindOne(ctx, id)
		if err != nil {
			return nil, err
		}

		// 处理缓存逻辑
		kubeNovaSysUserPlatformIdKey := fmt.Sprintf("%s%v", cacheKubeNovaSysUserPlatformIdPrefix, id)
		kubeNovaSysUserPlatformUserIdPlatformIdKey := fmt.Sprintf("%s%v:%v", cacheKubeNovaSysUserPlatformUserIdPlatformIdPrefix, data.UserId, data.PlatformId)
		// 执行带缓存处理的 SQL 操作
		return m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (sql.Result, error) {
			return conn.ExecCtx(ctx, query, args...)
		}, kubeNovaSysUserPlatformIdKey, kubeNovaSysUserPlatformUserIdPlatformIdKey)

	}

	// 如果 id 为零值，直接执行 SQL（无需处理缓存）

	return m.ExecNoCacheCtx(ctx, query, args...)

}

func (m *defaultSysUserPlatformModel) FindOne(ctx context.Context, id uint64) (*SysUserPlatform, error) {
	kubeNovaSysUserPlatformIdKey := fmt.Sprintf("%s%v", cacheKubeNovaSysUserPlatformIdPrefix, id)
	var resp SysUserPlatform
	err := m.QueryRowCtx(ctx, &resp, kubeNovaSysUserPlatformIdKey, func(ctx context.Context, conn sqlx.SqlConn, v any) error {
		query := fmt.Sprintf("select %s from %s where `id` = ? limit 1", sysUserPlatformRows, m.table)
		return conn.QueryRowCtx(ctx, v, query, id)
	})
	switch err {
	case nil:
		return &resp, nil
	case sqlc.ErrNotFound:
		return nil, ErrNotFound
	default:
		return nil, err
	}
}

func (m *defaultSysUserPlatformModel) Search(ctx context.Context, orderStr string, isAsc bool, page, pageSize uint64, queryStr string, args ...any) ([]*SysUserPlatform, uint64, error) {
	// 确保分页参数有效
	if page < 1 {
		page = 1
	}
	if pageSize < 1 {
		pageSize = 20
	}

	// 构造查询条件
	// 添加 is_deleted = 0 条件，保证只查询未软删除数据
	// 初始化 WHERE 子句，根据数据库类型选择正确的引用符号

	where := "WHERE `is_deleted` = 0"
	if queryStr != "" {
		where = fmt.Sprintf("WHERE %s AND `is_deleted` = 0", queryStr)
	}

	// 根据 isAsc 参数确定排序方式
	sortDirection := "ASC"
	if !isAsc {
		sortDirection = "DESC"
	}

	// 安全处理排序字段，防止 SQL 注入
	// 只允许字母、数字、下划线，并移除危险字符
	if orderStr == "" {
		orderStr = fmt.Sprintf("ORDER BY id %s", sortDirection)
	} else {
		orderStr = strings.TrimSpace(orderStr)
		// 移除可能的 SQL 注入字符：分号、注释符号、引号等
		orderStr = strings.NewReplacer(
			";", "",
			"--", "",
			"/*", "",
			"*/", "",
			"'", "",
			"\"", "",
			"\\", "",
		).Replace(orderStr)

		if !strings.HasPrefix(strings.ToUpper(orderStr), "ORDER BY") {
			orderStr = "ORDER BY " + orderStr
		}
		orderStr = fmt.Sprintf("%s %s", orderStr, sortDirection)
	}

	countQuery := fmt.Sprintf("SELECT COUNT(1) FROM %s %s", m.table, where)

	var total uint64
	var resp []*SysUserPlatform
	err := m.QueryRowNoCacheCtx(ctx, &total, countQuery, args...)
	if err != nil {
		return nil, 0, err
	}
	// 搜索无结果是正常情况，返回空切片而非错误
	// 这样上层调用方可以直接判断切片长度，无需额外处理错误
	if total == 0 {
		return []*SysUserPlatform{}, 0, nil
	}
	offset := (page - 1) * pageSize

	dataQuery := fmt.Sprintf("SELECT %s FROM %s %s %s LIMIT %d,%d", sysUserPlatformRows, m.table, where, orderStr, offset, pageSize)

	err = m.QueryRowsNoCacheCtx(ctx, &resp, dataQuery, args...)
	if err != nil {
		return nil, 0, err
	}

	return resp, total, nil
}

func (m *defaultSysUserPlatformModel) SearchNoPage(ctx context.Context, orderStr string, isAsc bool, queryStr string, args ...any) ([]*SysUserPlatform, error) {
	// 处理 nil 参数，确保后续操作安全
	if args == nil {
		args = []any{}
	}

	// 初始化 WHERE 子句，根据数据库类型选择正确的引用符号

	where := "WHERE `is_deleted` = 0"

	// 处理查询条件和参数
	var finalArgs []any
	if queryStr != "" {

		where = fmt.Sprintf("WHERE %s AND `is_deleted` = 0", queryStr)

		// 只有当有查询条件时才使用参数
		if len(args) > 0 {
			finalArgs = args
		}
	}

	// 根据 isAsc 参数确定排序方式
	sortDirection := "ASC"
	if !isAsc {
		sortDirection = "DESC"
	}

	// 安全处理排序字段，防止 SQL 注入
	if orderStr == "" {
		orderStr = fmt.Sprintf("ORDER BY id %s", sortDirection)
	} else {
		orderStr = strings.TrimSpace(orderStr)
		// 移除可能的 SQL 注入字符
		orderStr = strings.NewReplacer(
			";", "",
			"--", "",
			"/*", "",
			"*/", "",
			"'", "",
			"\"", "",
			"\\", "",
		).Replace(orderStr)

		if !strings.HasPrefix(strings.ToUpper(orderStr), "ORDER BY") {
			orderStr = "ORDER BY " + orderStr
		}
		orderStr = fmt.Sprintf("%s %s", orderStr, sortDirection)
	}

	dataQuery := fmt.Sprintf("SELECT %s FROM %s %s %s", sysUserPlatformRows, m.table, where, orderStr)
	var resp []*SysUserPlatform

	// 根据是否有参数来调用，避免传递空参数导致问题
	var err error
	if len(finalArgs) > 0 {
		err = m.QueryRowsNoCacheCtx(ctx, &resp, dataQuery, finalArgs...)
	} else {
		err = m.QueryRowsNoCacheCtx(ctx, &resp, dataQuery)
	}

	switch err {
	case nil:
		// 返回结果，如果无数据则返回空切片
		if resp == nil {
			return []*SysUserPlatform{}, nil
		}
		return resp, nil
	case sqlx.ErrNotFound:
		// 无数据时返回空切片而非错误，保持与 Search 方法一致的行为
		return []*SysUserPlatform{}, nil
	default:
		return nil, err
	}
}

func (m *defaultSysUserPlatformModel) FindOneByUserIdPlatformId(ctx context.Context, userId uint64, platformId uint64) (*SysUserPlatform, error) {
	kubeNovaSysUserPlatformUserIdPlatformIdKey := fmt.Sprintf("%s%v:%v", cacheKubeNovaSysUserPlatformUserIdPlatformIdPrefix, userId, platformId)
	var resp SysUserPlatform
	err := m.QueryRowIndexCtx(ctx, &resp, kubeNovaSysUserPlatformUserIdPlatformIdKey, m.formatPrimary, func(ctx context.Context, conn sqlx.SqlConn, v any) (i any, e error) {
		query := fmt.Sprintf("select %s from %s where `user_id` = ? and `platform_id` = ? limit 1", sysUserPlatformRows, m.table)
		if err := conn.QueryRowCtx(ctx, &resp, query, userId, platformId); err != nil {
			return nil, err
		}
		return resp.Id, nil
	}, m.queryPrimary)
	switch err {
	case nil:
		return &resp, nil
	case sqlc.ErrNotFound:
		return nil, ErrNotFound
	default:
		return nil, err
	}
}

func (m *defaultSysUserPlatformModel) Insert(ctx context.Context, data *SysUserPlatform) (sql.Result, error) {
	kubeNovaSysUserPlatformIdKey := fmt.Sprintf("%s%v", cacheKubeNovaSysUserPlatformIdPrefix, data.Id)
	kubeNovaSysUserPlatformUserIdPlatformIdKey := fmt.Sprintf("%s%v:%v", cacheKubeNovaSysUserPlatformUserIdPlatformIdPrefix, data.UserId, data.PlatformId)
	ret, err := m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (result sql.Result, err error) {
		query := fmt.Sprintf("insert into %s (%s) values (?, ?, ?, ?, ?, ?, ?)", m.table, sysUserPlatformRowsExpectAutoSet)
		return conn.ExecCtx(ctx, query, data.UserId, data.PlatformId, data.IsEnable, data.Status, data.IsDeleted, data.CreateBy, data.UpdateBy)
	}, kubeNovaSysUserPlatformIdKey, kubeNovaSysUserPlatformUserIdPlatformIdKey)
	return ret, err
}

func (m *defaultSysUserPlatformModel) Update(ctx context.Context, newData *SysUserPlatform) error {
	data, err := m.FindOne(ctx, newData.Id)
	if err != nil {
		return err
	}

	kubeNovaSysUserPlatformIdKey := fmt.Sprintf("%s%v", cacheKubeNovaSysUserPlatformIdPrefix, data.Id)
	kubeNovaSysUserPlatformUserIdPlatformIdKey := fmt.Sprintf("%s%v:%v", cacheKubeNovaSysUserPlatformUserIdPlatformIdPrefix, data.UserId, data.PlatformId)
	_, err = m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (result sql.Result, err error) {
		query := fmt.Sprintf("update %s set %s where `id` = ?", m.table, sysUserPlatformRowsWithPlaceHolder)
		return conn.ExecCtx(ctx, query, newData.UserId, newData.PlatformId, newData.IsEnable, newData.Status, newData.IsDeleted, newData.CreateBy, newData.UpdateBy, newData.Id)
	}, kubeNovaSysUserPlatformIdKey, kubeNovaSysUserPlatformUserIdPlatformIdKey)
	return err
}

func (m *defaultSysUserPlatformModel) formatPrimary(primary any) string {
	return fmt.Sprintf("%s%v", cacheKubeNovaSysUserPlatformIdPrefix, primary)
}

func (m *defaultSysUserPlatformModel) queryPrimary(ctx context.Context, conn sqlx.SqlConn, v, primary any) error {
	query := fmt.Sprintf("select %s from %s where `id` = ? limit 1", sysUserPlatformRows, m.table)
	return conn.QueryRowCtx(ctx, v, query, primary)
}

func (m *defaultSysUserPlatformModel) tableName() string {
	return m.table
}
